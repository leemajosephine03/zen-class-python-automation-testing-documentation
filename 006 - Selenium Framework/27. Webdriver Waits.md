## 27. Webdriver Waits

* Python Selenium Waits
* Implicit Wait
* Explicit Wait
* Fluent Wait
* Expected conditions support

## Python Selenium Waits
* In Selenium, the sleep() method pauses the execution of a test for a specified period of time.
* It's a method provided by Python for Handling dynamic elements and resolving timing issues.
* It also have some drawbacks, including longer testing time.
* Inorder to overcome these issues, selenium have webdriver waits.
* These waits introduce pauses during test execution, allowing the script to wait until specific conditions are met.

## Types of Selenium Waits
* Implicit Wait
* Explicit Wait
* Fluent Wait

## 1. Implicit Wait
* Implicit Wait in Selenium is a global wait that applies to all elements in the script.
* It sets a maximum wait time for any element to become available before interaction.
* If the element appears within the specified time, the script continues; otherwise, it raises a **TimeoutException**.

~~~python
"""
Python Selenium Expected Conditions - IMPLICIT WAIT
"""
from selenium import webdriver
from selenium.webdriver.chrome.service import Service
from webdriver_manager.chrome import ChromeDriverManager
from selenium.webdriver.common.by import By
from selenium.common.exceptions import NoSuchElementException
from selenium.common.exceptions import ElementNotVisibleException


class Data:
    username = "tom harris"
    password = "Secret@123"


class Locators:
    username_input_box = "email"
    password_input_box = "pass"
    submit_button = "login"


class ImplicitWait(Data, Locators):

    def __init__(self, web_url):
        self.url = web_url
        self.driver = webdriver.Chrome(service=Service(ChromeDriverManager().install()))


    def start(self):
        try:
            self.driver.implicitly_wait(10)
            self.driver.maximize_window()
            self.driver.get(self.url)
            username = self.driver.find_element(by=By.ID, value=self.username_input_box)
            password = self.driver.find_element(by=By.ID, value=self.password_input_box)
            submit_button = self.driver.find_element(by=By.NAME, value=self.submit_button)
            if username.is_displayed() and password.is_displayed() and submit_button.is_enabled():
                username.send_keys(self.username)
                password.send_keys(self.password)
                submit_button.click()
                print("SUCCESS : Automated Facebook Page")
        except (NoSuchElementException, ElementNotVisibleException) as error:
            print(error)


url = "https://www.facebook.com/"
implicit_wait = ImplicitWait(url)
implicit_wait.start()

~~~

## 2. Explicit Wait
* Explicit Wait allows you to wait for a specific condition or element.
* Explicit waits are applied to individual elements and provide better control.
* It often uses the WebDriverWait class and expected conditions to define custom waiting criteria.

## Types of Expected Conditions/Support
* First we need to import ExpectedConditions class

**Element Visibility:** The visibilityOfElementLocated() condition ensures that an element is present in the DOM and visible on the page.

**Element Clickability:** The elementToBeClickable() condition is crucial for scenarios where an element needs interaction, such as clicking a button.

**Text to Be Present In Element:** The textToBePresentInElementLocated() condition ensures that the text is present in a specified element.

**Presence of Elements:** The presenceOfElementLocated() condition waits for at least one element to be present in the DOM.

**The invisibility of Elements:** The invisibilityOfElementLocated() condition waits for the specified element to be invisible.

~~~python
"""
Python Selenium Expected Conditions - EXPLICIT WAIT
"""
from selenium import webdriver
from selenium.webdriver.chrome.service import Service
from webdriver_manager.chrome import ChromeDriverManager
from selenium.webdriver.common.by import By
from selenium.common.exceptions import NoSuchElementException
from selenium.common.exceptions import ElementNotVisibleException
# For Explicit Wait
from selenium.webdriver.support.wait import WebDriverWait
from selenium.webdriver.support import expected_conditions as EC


class Data:
   username = "Richard"
   password = "Secret@123"


class Locators:
   username_input_box = "email"
   password_input_box = "pass"
   submit_button = "login"


class ExplicitWait(Data, Locators):

    def __init__(self, web_url):
        self.url = web_url
        self.driver = webdriver.Chrome(service=Service(ChromeDriverManager().install()))
        self.wait = WebDriverWait(self.driver, 10)


    def login(self):
        try:
            self.driver.maximize_window()
            self.driver.get(self.url)
            self.wait.until(EC.presence_of_element_located((By.ID, self.username_input_box))).send_keys(self.username)
            self.wait.until(EC.presence_of_element_located((By.ID, self.password_input_box))).send_keys(self.password)
            self.wait.until(EC.element_to_be_clickable((By.NAME, self.submit_button))).click()
            print("SUCCESS : Automated Facebook Page!")
          
        except (NoSuchElementException, ElementNotVisibleException) as error:
            print(error)
          
        finally:
            self.driver.quit()

url = "https://www.facebook.com/"
explicit_wait = ExplicitWait(url)
explicit_wait.login()

~~~

## Mini Project/ Session Activities
~~~python
"""
MINI PROJECT : Python Selenium EXPLICIT WAIT with ChromeOptions 640 x 450
"""
from selenium import webdriver
from selenium.webdriver.chrome.service import Service
from webdriver_manager.chrome import ChromeDriverManager
from selenium.webdriver.common.by import By
from selenium.common.exceptions import NoSuchElementException
from selenium.common.exceptions import ElementNotVisibleException
# For Explicit Wait
from selenium.webdriver.support.wait import WebDriverWait
from selenium.webdriver.support import expected_conditions as EC
# Import ChromeOptions
from selenium.webdriver.chrome.options import Options


class Data:
   url = "https://www.facebook.com/"
   username = "Anderson"
   password = "Secret@123"


class Locators:
   username_input_box = "email"
   password_input_box = "pass"
   submit_button = "login"


class ExplicitWait(Data, Locators):

   
    def __init__(self, web_url):
        # chrome options
        self.chrome_options = Options()
        self.chrome_options.add_argument(f"--window-size={640},{450}")
        # url
        self.url = web_url
        # webdriver and wait
        self.driver = webdriver.Chrome(service=Service(ChromeDriverManager().install()), options=self.chrome_options)
        self.wait = WebDriverWait(self.driver, 10)


    def login(self):
        try:
            self.driver.get(self.url)
            self.wait.until(EC.presence_of_element_located((By.ID, self.username_input_box))).send_keys(self.username)
            self.wait.until(EC.presence_of_element_located((By.ID, self.password_input_box))).send_keys(self.password)
            self.wait.until(EC.element_to_be_clickable((By.NAME, self.submit_button))).click()
            window = self.driver.get_window_size()
            window_width =  window['width']
            window_height = window['height']
            print(f"SUCCESS : Automated Facebook Page with window size of {window_width} x {window_height}")
            

        except (NoSuchElementException, ElementNotVisibleException) as error:
            print(error)


        finally:
           self.driver.quit()


url = "https://www.facebook.com/"
explicit_wait = ExplicitWait(url)
explicit_wait.login()

~~~

## 3. Fluent Wait
* Another type of explicit wait.
* Fluent Wait in Selenium allows one to wait for a specific condition to be met with a custom frequency of checking the condition.
* It allows us to define the polling frequency and exceptions to ignore during the waiting period.
* Need to import WebDriverWait and Expected Conditions from selenium.
* Polling frequency specifies how frequently Selenium should check the condition to be met before timing out.

~~~python
"""
Python Selenium - FLUENT WAIT
"""
from selenium import webdriver
from selenium.webdriver.chrome.service import Service
from webdriver_manager.chrome import ChromeDriverManager
from selenium.webdriver.common.by import By
from selenium.common.exceptions import NoSuchElementException
from selenium.common.exceptions import ElementNotVisibleException
# For Explicit Wait
from selenium.webdriver.support.wait import WebDriverWait
from selenium.webdriver.support import expected_conditions as EC


class Data:
   username = "mark"
   password = "Secret@123"


class Locators:
   username_input_box = "email"
   password_input_box = "pass"
   submit_button = "login"


class SumanFluentWait(Data, Locators):

    def __init__(self, web_url):
        self.url = web_url
        self.driver = webdriver.Chrome(service=Service(ChromeDriverManager().install()))
        self.wait = WebDriverWait(self.driver, 10)
        self.ignored_exceptions = [NoSuchElementException, ElementNotVisibleException]
        self.wait = WebDriverWait(self.driver, 10, poll_frequency=5, ignored_exceptions=self.ignored_exceptions)


    def login(self):
        try:
            self.driver.maximize_window()
            self.driver.get(self.url)
            self.wait.until(EC.presence_of_element_located((By.ID, self.username_input_box))).send_keys(self.username)
            self.wait.until(EC.presence_of_element_located((By.ID, self.password_input_box))).send_keys(self.password)
            self.wait.until(EC.element_to_be_clickable((By.NAME, self.submit_button))).click()
            print("SUCCESS : Automated Facebook Page!")
            
        except (NoSuchElementException, ElementNotVisibleException) as error:
            print(error)
        finally:
            self.driver.quit()


url = "https://www.facebook.com/"
suman = SumanFluentWait(url)
suman.login()

~~~
